import type { Express } from "express";

export function registerAuthRoutes(app: Express) {
  // Auth routes podem ser adicionadas aqui no futuro (login, logout, refresh)
  // Por enquanto, apenas o middleware é usado
}
